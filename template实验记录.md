# 实验 E<编号> · <一句话假设>

> **本文件是模板。** 复制它 → 改名 `experiments/E01-<简述>.md` → 填内容。
> **一次实验 = 一个文件。** 没有实验记录的结论，不算结论。
> 建立：2026-09-15 ｜ **按实际用法对齐：2026-09-16**
>
> **参考实例**：`experiments/E01-baseline.md`（已跑过的）·
> `experiments/E04-数据集对照.md`（**只有方案、还没跑**的）
>
> ### ⚠️ 模板有边界 —— 三种情况别硬套
>
> | 情况 | 做法 |
> |---|---|
> | **已跑过的实验** | 套全七节 |
> | **只有方案、还没跑** | 照套，但**未跑项一律留 ⬜，一个数都不许预填** |
> | **是「测量方法」不是「实验」** | ⛔ **套不了** —— 见 `experiments/E05-人工判断.md`，它另立结构。<br>**不要为了套模板而把方法硬塞进「假设/变量/结果」** |
>
> ### ⚠️ 两条最容易违反的
>
> - **§二「一次只改一个」** —— 本库踩过：E03 同时改了 `lr` 和 `epochs`，**归因不成立**。
>   另注意**「语言」也是数据的一部分**，换数据集时语言跟着变，等于改了不止一个变量。
> - **§五「对照样例」不能空着** —— `training loss 不是结论`。若确实没有输出质量证据，
>   **就在 §五 明写「没有」**，并把它列进 §七 的缺口 —— **不要跳过这一节。**

---

## 一 · 假设

> **一句话**：我改 ______，预期 ______ 会变 ______，因为 ______。

- **本实验属于哪一类**（二选一，**这个仓库的价值就在这里**）：
  - [ ] **改数据**（数据集内容 / 配比 / 清洗规则）
  - [ ] **改参数**（lr / rank / epochs / target_modules …）
  - [ ] 其他：______

## 二 · 变量

| | 值 |
|---|---|
| **自变量**（只改这一个） | |
| 对照组 | |
| 控制变量（保持不变的） | |

> ⚠️ **一次只改一个。** 同时改两个，就分不清是哪个起的作用 —— 实验白做。

## 三 · 环境

| 项 | 值 |
|---|---|
| 硬件 | （本机 / AutoDL · 卡型 · 显存） |
| 基座模型 | |
| 微调框架 | （LLaMA-Factory 版本） |
| 关键依赖版本 | |
| 训练时长 / 成本 | |

## 四 · 配置

- 配置文件路径：`configs/______`
- 关键超参：

| 参数 | 值 |
|---|---|
| learning_rate | |
| lora_rank | |
| lora_alpha | |
| epochs | |
| batch_size | |
| cutoff_len | |

## 五 · 结果

| 指标 | 对照组 | 实验组 | 变化 |
|---|---|---|---|
| 训练 loss（末值） | | | |
| 评测分数 | | | |
| **人工判断**（关键） | | | |

> ⚠️ **训练 loss 不是结论。** loss 降了但输出质量没变 —— **这正是本项目已观察到的现象**。
> **必须附人工判断**，至少给 3 组对照样例（贴进 `results/`）。

**对照样例**：

```
【输入】
（同一 prompt）

【对照组输出】

【实验组输出】

【判断】哪个更好？为什么？
```

## 六 · 结论

- **假设成立吗**：成立 / 不成立 / 无法判断
- **一句话结论**：
- **意外发现**：
- **下一步**：

---

## 七 · 可复现性检查（**发布前必须全勾**）

- [ ] 配置文件已进仓库
- [ ] 数据卡已更新（若动了数据）
- [ ] 脚本能一键重跑
- [ ] 上表中每个数字都能从仓库文件复算出来
- [ ] 若结论进了 README，**已标注实验编号**
