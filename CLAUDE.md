# lora-finetune · 工作规约

> 在这个文件夹里开 Claude 时，先读本文件。
> 建立：2026-09-15 ｜ 重定位 2026-09-16 ｜ **按实际内容对齐：2026-09-16**
>
> 📌 **本文件描述的是「现在实际是什么样」，不是「当初打算建成什么样」。**
> 早先按模板写的结构若有出入，以本文件与仓库实际为准。

---

## 这是什么

**一个 LoRA 微调的「数据与实验库」** —— 不是单次项目，是**历史数据的真实存放处**。

1. **它是库** —— 每次 LoRA 工作的**数据集、训练配置、实验记录、评测结果**都进这里。
   **以后还会有其他 LoRA 数据进来**，所以结构必须**横向可加**，
   ⛔ 不要写死「只有这一批数据」的假设。
2. **它是历史留档** —— 数据怎么造的、参数怎么选的、哪次成了哪次废了，都留在这儿。
3. **求职时能用到，是顺带结果，不是目的** —— 留档第一，展示第二。

> 📌 2026-09-16 定位变更：此前本仓库被定义为「求职证据」并配有一份 JD 对照清单，
> **该文件与求职框架已移出仓库**（`~/Desktop/jobsearch求职工作区/job求职/90_inbox待归类/`）。
> **本仓库文档中不再出现求职 / JD / 面试 / 作品集这类框架。**

---

## 🔴 第一铁律：不编数字

**本仓库里出现的每一个数字，都必须可追溯到一次真实的运行。**

- 训练 loss、评测分数、数据集条数、显存占用、推理延迟 —— **全部要能从仓库里的文件复算**
- **做不到复算的，就写「未记录」，不要写一个看起来合理的数**
- 训练日志 / 配置 / 评测脚本输出**要一起进仓库**，否则数字没有凭据

> **为什么排在第一位**：没有外部裁判时，模型（和人）会用**默认值**把空缺填满，
> 而**默认值在表上跟真实观测长得一模一样**。掺了编的数字，仓库就不再是证据，而是负债。

**⚠️ 三条实操要求**（都是踩过坑之后加的）：
1. **推断与实测必须区分写** —— 写「⚠️ 推断」+ 依据，或直接写「未记录」。**不许默默填一个值。**
2. **预期值不得混入结果表** —— 推算、举例、示意、别人的说法，一律不进结果表。
   **同一张表也不许改标题复用**（把「预期结果」改成「结果」再列一遍、数值不动，是最难发现的一类假数据）。
3. **「内容更全」≠「实际跑过的那版」** —— 本库踩过：合并时选了内容更全的配置，
   但产物证明实际跑的是另一个版本。**判断依据要看产物，不看文件的完整度。**

---

## 🔴 第二铁律：这是公开仓库

- **不含个人信息**：不写真实姓名/电话/邮箱/住址/公司内网地址
  （`LICENSE` 署名为 GitHub 用户名 `heweidong-ecco`，非真名）
- **不含受限数据**：来源不可公开的数据集，**不放原始数据**，只放**数据卡**
- **不含密钥**：一律走环境变量，**绝不进仓库**
  > 📌 曾在本仓库前身发现**真实泄漏的 API_KEY**（`.env` + 12 处硬编码），已全部清除。
  > 新增代码请用 `os.getenv("API_KEY")`，**不要写默认值**。
- **不含模型权重**：LoRA adapter 放 HuggingFace / 魔搭，仓库只放链接 + 配置 + **可复算的小文本产物**

---

## 一 · 核心判断（**尚未验证**）

> **LoRA 的效果，主要由数据集质量决定，不是由超参数决定。**

本项目的经验判断，原话：

> *"LoRA 的好坏 80% 来自数据集，一直调参只是改变曲线和数值，无法改变输出结果的质量；
> 数据集的好坏对 LoRA 影响很大。"*

### ⚠️ 现状：**这个判断还没有被本库验证过**

已跑过的 **E01 / E02 / E03 三组全部在改参数，一次改数据的都没有** ——
它们只能说明「这两次调参没有收益」，**支撑不了上面那句话**。

**要验证它，需要 E04（固定全部参数，只换数据集）** —— ⬜ **方案已就绪，待跑**。

> ⛔ **不要在任何文档、注释、对话里把这个判断写成「已证明」。**
> 详见 `experiments/README.md`。

---

## 二 · 目录结构（**与仓库实际一致**）

```
lora-finetune/
├── README.md                     库总说明（对外）
├── CLAUDE.md                     本文件
├── LICENSE                       MIT（**只覆盖代码**；数据另有许可）
├── .gitignore
├── template数据卡.md              数据集描述模板
├── template实验记录.md            单次实验记录模板
│
├── datasets/                     ⭐ 库的核心增长轴
│   ├── README.md                 数据集总索引（新数据进来先登记这里）
│   ├── alpaca-clean-500条/       数据卡 + 500/50 数据 + E04 两组 194 条
│   ├── qa-general-194条/         数据卡 + 194 条 + 质检报告
│   └── code-completion代码补全/   数据卡（**数据文件已撤出**，来源未确认）
│
├── configs/                      训练配置（9 个 + README + 优化策略）
├── scripts/
│   ├── README.md                 脚本索引（含三个已查实缺陷的说明）
│   ├── datasets/                 数据工程
│   └── eval/                     评测
│
├── experiments/
│   ├── README.md                 实验总览（真库表 + 缺口清单）
│   ├── E01~E03                   已跑过的三组（改参数）
│   ├── E04-数据集对照.md          ⬜ 方案已就绪，待跑（改数据）
│   ├── E05-人工判断.md            ⬜ 方案 + 脚本已就绪，待跑（测量方法）
│   └── artifacts训练产物/        五组训练的小文本产物（**可复算的依据**）
│       └── README.md             读数前必看：三个读数陷阱
│
├── results/                      评测产物 + README.md（现有评测不可用，已写明）
├── deploy/                       GGUF 导出 / Ollama 部署 + README.md
├── subprojects/                  两个子方向 + README.md（**均无实验记录**）
├── gateway/                      FastAPI 推理网关（27 文件）
└── docs/                         溯源与说明 + README.md
```

> ⚠️ **`datasets/` 是增长轴。** 新数据进来 = **新增一个子目录 + 登记索引**，不改动已有目录。
> **`datasets/` `configs/` `experiments/` `results/` 必须能互相对应上** ——
> 否则「哪个数字来自哪次实验」就断了，第一铁律守不住。

---

## 三 · 命名规范

> **采用规范 ①：`英文中文-限定词`**（英文功能词 + 中文释义，允许中英混用）。
> 用户 2026-09-16 从 `Product/CLAUDE.md` §四 的三套规范中裁决选定。
> ⛔ **不要自己换一套，也不要中英随意拼。**

| 类型 | 实例 |
|---|---|
| 数据集目录 | `alpaca-clean-500条/` · `qa-general-194条/` · `code-completion代码补全/` |
| 数据卡 | `数据卡-alpaca500.md` · `数据卡-qa194.md` · `数据卡-codecompletion.md` |
| 索引 | **统一用 `README.md`** —— GitHub 浏览目录时会**自动渲染**它（2026-09-16 由 `readme索引.md` 统一改来） |
| 脚本目录 | `scripts/datasets/` · `scripts/eval/` |
| 实验记录 | `E01-baseline.md`（`E<编号>-<简述>`） |
| 运行产物 | `artifacts训练产物/` |
| 配置 | `E04-A组-random随机194.yaml` |

---

## 四 · 工作方式

1. **一个数据集 = 一份数据卡** —— 从 `template数据卡.md` 复制，放**该数据集自己的目录**
2. **一次实验 = 一个 `experiments/` 文件** —— 从 `template实验记录.md` 复制
3. **结论只写在 README 和 `results/`**，且**每条结论必须指回具体实验编号**
4. 改超参之前先问：**这次改的是数据还是参数？** —— 本库的价值就在「分清这两者」
5. **新数据集登记流程**见 `datasets/README.md` §三

### ⚠️ 模板有边界，不是每个文件都套

| 情况 | 做法 |
|---|---|
| 已跑过的实验（E01~E03） | 套 `template实验记录.md` 全七节 |
| **只有方案、还没跑**（E04） | 套模板，但**未跑项一律留 ⬜，不许预填** |
| **是方法不是实验**（E05） | ⚠️ **套不了模板** —— 它是测量口径，另立结构，**不要硬塞** |
| 数据集**来源未确认**（code-completion） | 数据卡照写，**§五 缺陷里必须写明「来源不确定」** |

> 📌 模板是**脚手架**。当实际内容装不进模板时，**改模板或另立结构，不要为了套模板而削内容。**

---

## 五 · 待办（**2026-09-16 清账，已去重**）

### A · 内容缺口 —— **必须真实运行才能补，写文档补不了**

| # | 事项 | 状态 |
|---|---|---|
| 1 | **E04：固定参数，只换数据集** —— 核心判断的唯一支撑 | ⬜ **方案已就绪**，缺 GPU |
| 2 | **E05：人工判断 / 对照样例** —— 没有它，loss 变不成质量结论 | ⬜ **方案 + 脚本已就绪**，缺 GPU |
| 3 | `subprojects/` 两个子方向（查询改写 / 代码补全）**均无实验记录** | ⬜ |
| 4 | `code-completion代码补全` 的**来源确认** + 许可证判定 | ⬜ 数据文件已按最保守方案撤出 |
| 5 | `deploy/` **无部署结果记录**（导出过哪些 GGUF、量化前后差多少） | ⬜ |

### B · 工程项 —— **已完成**（保留作台账，勿重复做）

| 事项 | 完成于 |
|---|---|
| ✅ `LICENSE`（MIT，署名 `heweidong-ecco`） | 2026-09-16 |
| ✅ 推 GitHub 公开仓库：`github.com/heweidong-ecco/lora-finetune` | 2026-09-16 |
| ✅ 清除泄漏的 `API_KEY`（`.env` + 12 处硬编码，共 36 处） | 2026-09-16 |
| ✅ 五组训练产物 **小文本文件**收进仓库（`experiments/artifacts训练产物/`，110 文件 / 约 0.5 MB） | 2026-09-16 |
| ✅ `gateway/` 13 个文件名清理（去 `LoRA更新后的` 前缀、补扩展名、修双后缀） | 2026-09-16 |
| ✅ `gateway/` 文档**补回丢失的代码围栏**（9 文件 / 85 段，已验证正文零改动） | 2026-09-16 |
| ✅ 9 个目录**全部有索引**，并统一为 `README.md`（GitHub 目录浏览会自动渲染） | 2026-09-16 |
| ✅ 修正 `configs/exp1_baseline.yaml` 为**实际跑过的那版**（原收录的是改过但没跑过的） | 2026-09-16 |

### C · 收尾时留意

- ⚠️ **推送前先确认 `.gitignore` 生效**再 `git add`（原始素材在仓库外的归档里）
- ⚠️ 归档位置：`~/Desktop/Product/lora-finetune-原始文件归档/`（约 161 MB，**仓库外**）
- ⚠️ **本文件 §五 会过期** —— 改动结构或完成待办后，**回来改它**，别让它变成误导
