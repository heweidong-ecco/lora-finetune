# lora-finetune · 工作规约

> 在这个文件夹里开 Claude 时，先读本文件。
> 建立：2026-09-15 ｜ **重定位：2026-09-16**（从「求职作品集」改为「数据与实验库」）

---

## 这是什么

**一个 LoRA 微调的「数据与实验库」** —— 不是一个单次项目，是**历史数据的真实存放处**。

三条定位，按重要性排序：

1. **它是库** —— 每次 LoRA 工作的**数据集、训练配置、实验记录、评测结果**都进这里，
   **以后还会有其他的 LoRA 数据进来**。所以结构必须**横向可加**，
   ⛔ **不要写死"只有这一批数据"的假设**。
2. **它是历史留档** —— 数据怎么造的、参数怎么选的、哪次成了哪次废了，都留在这儿。
3. **求职时能用到，是顺带的结果，不是目的** ——
   ⛔ **不要为了"面试好看"去写任何东西**。留档第一，展示第二。

> 📌 **2026-09-16 的定位变更**：此前本仓库被定义为「求职证据」，并配有一份
> `requirement需求-证据清单.md` 对接 JD。**该文件与求职框架已移出本仓库**
> （现在 `~/Desktop/jobsearch求职工作区/job求职/90_inbox待归类/`）。
> **本仓库的文档中不再出现求职 / JD / 面试 / 作品集这类框架。**

---

## 🔴 第一铁律：不编数字

**本仓库里出现的每一个数字，都必须可追溯到一次真实的运行。**

- 训练 loss、评测分数、数据集条数、显存占用、推理延迟 —— **全部要能从仓库里的文件复算出来**
- **做不到复算的，就写「未记录」，不要写一个看起来合理的数**
- 训练日志 / 配置文件 / 评测脚本的输出**要一起进仓库**，否则数字没有凭据

> **为什么这条排在第一位**：本项目的元规则已经吃过一次亏 ——
> 没有外部裁判时，模型（和人）会用**默认值**把空缺填满，而**默认值在表上跟真实观测长得一模一样**。
> 一个仓库里掺了编的数字，它就不再是证据，而是负债。

**⚠️ 特别提醒**：**推断**和**实测**必须区分开写。
本仓库里已有多处「⚠️ 推断」（如硬件型号、实验日期、框架版本），
都标了推断依据。**不要把它们当成实测值引用。**

---

## 🔴 第二铁律：这是公开仓库

- **不含任何个人信息**：不写真实姓名/电话/邮箱/住址/公司内网地址
- **不含受限数据**：数据集若来源不可公开，**不放原始数据**，只放「数据卡」（见 `template数据卡.md`）
- **不含密钥**：AutoDL token、API key、HF token —— 一律走环境变量，**绝不进仓库**
  > 📌 2026-09-16 曾在本仓库前身里发现一个真实泄漏的 `API_KEY`（`.env` + 12 处硬编码），
  > **已全部清除**。新增代码时请勿再犯：一律 `os.getenv("API_KEY")`，**不要写默认值**。
- **不含模型权重**：LoRA adapter 体积大且无必要，**放 HuggingFace / 魔搭**，仓库里只放链接 + 训练配置

---

## 一 · 核心论点（**目前尚未验证**）

> **LoRA 的效果，主要由数据集质量决定，不是由超参数决定。**

这是本项目**的经验判断**，原话：

> *"LoRA 的好坏 80% 来自数据集，一直调参只是改变曲线和数值，无法改变输出结果的质量；
> 数据集的好坏对 LoRA 影响很大。"*

### ⚠️ 现状：这个论点**还没有被本仓库验证过**

`experiments/` 里现有的 **E01 / E02 / E03 三组实验全部在「改参数」，一次「改数据」都没有**。
所以它们**支撑不了**上面这句话，只能说明「这两次调参没有收益」。

**要验证它，需要 E04：固定全部参数，只换数据集。** —— ⬜ **还没做。**

> ⛔ **不要在任何文档、注释、对话里把这个论点写成「已证明」。**
> 详见 `experiments/README.md`。

---

## 二 · 目录约定

```
lora-finetune/
├── README.md                    ← 库总说明（对外）
├── CLAUDE.md                    ← 本文件
├── LICENSE                      ← MIT（代码部分；数据部分另有许可）
├── .gitignore
├── template数据卡.md             ← 数据集描述模板
├── template实验记录.md           ← 单次实验记录模板
│
├── datasets/                    ← ⭐ 库的核心增长轴
│   ├── readme索引.md             ← 数据集总索引（新数据进来先登记这里）
│   └── <数据集名>/               ← 一个数据集一个目录
│       ├── 数据卡-<简称>.md
│       └── <数据文件>
│
├── configs/                     ← 训练配置
├── scripts/datasets/            ← 数据工程脚本
├── scripts/eval/                ← 评测脚本
├── experiments/                 ← 每次实验一份记录（由 template 复制）
├── results/                     ← 评测输出、对照表、图表
├── deploy/                      ← 量化导出 / Ollama 部署
├── subprojects/                 ← 独立的子方向（code-completion / rag-query-rewrite）
├── gateway/                     ← FastAPI 推理网关
└── docs/                        ← 溯源与说明
```

> ⚠️ **`datasets/` 是增长轴。** 新数据进来 = **新增一个子目录 + 登记索引**，不改动已有目录。
> **`datasets/` `configs/` `experiments/` `results/` 这四块必须能互相对应上** ——
> 否则「哪个数字来自哪次实验」就断了，第一铁律就守不住。

---

## 三 · 命名规范

> **本仓库采用规范 ①：`英文中文-限定词`**（英文功能词 + 中文释义，允许中英混用）。
> **这是用户 2026-09-16 从 `Product/CLAUDE.md` §四 的三套规范中裁决选的。**
> ⛔ **不要自己换一套，也不要中英随意拼。**

**实例**：

| 类型 | 例子 |
|---|---|
| 数据集目录 | `alpaca-clean-500条/` · `qa-general-194条/` · `code-completion代码补全/` |
| 数据卡 | `数据卡-alpaca500.md` · `数据卡-qa194.md` |
| 索引 | `readme索引.md` |
| 脚本目录 | `scripts/datasets/` · `scripts/eval/` |
| 实验记录 | `E01-baseline.md`（沿用模板的 `E<编号>-<简述>`） |

---

## 四 · 工作方式

1. **一次实验 = 一个 `experiments/` 文件**，从 `template实验记录.md` 复制
2. **一个数据集 = 一个数据卡**，从 `template数据卡.md` 复制，放**该数据集自己的目录**里
3. **结论只写在 README 和 `results/`**，且**每条结论必须指回具体实验编号**
4. 改超参之前，**先问：这次改的是数据还是参数？** —— 本仓库的价值就在"分清这两者"
5. **新数据集的登记流程**见 `datasets/readme索引.md` §三

### ⚠️ 写文档时的两条硬要求

- **推断必须标注**。写「⚠️ 推断」+ 依据，或直接写「未记录」。**不许默默填一个值。**
- **缺陷必须写**。数据卡的 §五「已知缺陷」**空着 = 该卡片算未完成**。

---

## 五 · 下一步（待办）

### 内容缺口（**这些需要真实运行，不是写文档能补的**）

- [ ] ⬜ **E04：固定参数，只换数据集** —— 核心论点的唯一支撑，**本仓库最大缺口**
- [ ] ⬜ **E05：人工判断 / 对照样例** —— 没有它，所有 loss 数字变不成质量结论
- [ ] ⬜ `code-completion代码补全` 的来源确认 + 许可证确认 + 训练实验记录
         （✅ 2026-09-16 已按最保守方案处置：**数据文件撤出仓库，只留数据卡**）

### 工程待办

- [ ] ⬜ 本仓库代码的 `LICENSE`（已建 MIT，待确认）
- [ ] ⬜ `git init` + 推 GitHub（**注意 `.gitignore` 已挡掉原始素材目录**）
- [x] ✅ `gateway/` 文件名已清理（2026-09-16，13 个文件改名）
- [ ] ⬜ `gateway/` 的文档**代码围栏丢失**（从对话粘贴所致，`test_main.md` 有 12 处裸 `bash`/`json` 标记），代码块不渲染
- [ ] ⬜ `LICENSE` 版权所有者已填 `heweidong-ecco`，请确认是否用这个账号
