# 数据卡 · <数据集名称>

> **本文件是模板。** 复制它 → 改名 `数据卡-<简称>.md` → 放进**该数据集自己的目录**。
> **为什么数据卡是本仓库的重心**：`CLAUDE.md` §一 的判断是「数据 ≫ 超参数」。
> 判断要成立，**数据集就必须被描述得足够细，细到能解释「为什么它有效」**。
> 建立：2026-09-15 ｜ **按实际用法对齐：2026-09-16**
>
> 📌 **对齐说明**：本模板原为六节，实际使用中三份数据卡都多出了 **§七 复现方式**
> → 已补进模板。**模板是脚手架，装不下实际内容时就改模板，不要削内容。**
>
> **参考实例**（照它们写）：`datasets/alpaca-clean-500条/数据卡-alpaca500.md` ·
> `datasets/qa-general-194条/数据卡-qa194.md` · `datasets/code-completion代码补全/数据卡-codecompletion.md`
>
> ⚠️ **模板有边界**：若某节与实际情况不符（例如来源未确认、根本没有版本历史），
> **写明原因**，不要为了填满而编。

---

## 一 · 基本信息

| 项 | 值 |
|---|---|
| 名称 / 版本 | |
| 来源 | 自建 / 公开数据集 / 混采 |
| 条数 | |
| 语言 | |
| 许可证 | **能否公开？** 能 / 否（不能则只留本卡片） |
| 原始数据位置 | 仓库内路径 / 外部链接 |

## 二 · 任务定义

- **要模型学会什么**：
- **输入格式**：
- **输出格式**：
- **一条样本长什么样**（贴一条脱敏样例）：

```
（样例）
```

## 三 · 构造过程（**这一节最关键**）

| 步骤 | 怎么做的 | 产出多少条 | 砍掉多少条 |
|---|---|---|---|
| 1. 采集 | | | |
| 2. 清洗 | | | |
| 3. 去重 | | | |
| 4. 格式化 | | | |
| 5. 人工校验 | | | |

- **清洗规则**（逐条列出，要能被别人复现）：
- **去重口径**（完全匹配 / 相似度阈值 / 语义）：
- **质检方式**（抽检比例？几个人？一致性怎么算？）：

> ⚠️ **"砍掉多少条 + 为什么砍" 比 "最后剩多少条" 更有说服力。**
> 它证明你**有判断标准**，不是无脑堆数据。

## 四 · 配比与统计

| 维度 | 分布 |
|---|---|
| 任务类型 | |
| 长度分布 | |
| 难度分布 | |
| 来源分布 | |

- **有没有刻意做过配比？** 有 / 无 —— 若做过，**配比前后各训一次**，结果记进 `experiments/`

## 五 · 已知缺陷（**必须写，不许省略**）

- **覆盖不到的场景**：
- **可能引入的偏见**：
- **已知的错标 / 噪声**：
- **不适合的用途**：

> 写不出缺陷 = 没真的检查过。**这一节空着，本卡片算未完成。**

## 六 · 版本历史

| 版本 | 日期 | 改了什么 | 为什么改 | 效果（指回实验号） |
|---|---|---|---|---|
| v1 | | | | |

## 七 · 复现方式

> ⚠️ **能被别人复现的数据集，才配写数据卡。** 这一节要给出**可执行的命令**，
> 而不是描述「我做了什么」。

```bash
# 按顺序列出实际跑过的命令，每条都标出产出什么
python scripts/datasets/______.py     # → ______.json（__ 条）
```

**可复现性要说清是哪一种**：

| 类型 | 含义 | 例子 |
|---|---|---|
| **可精确复现** | 固定种子，重跑得到**逐条相同**的结果 | `prepare_data.py`（`random.seed(42)`） |
| **只能复现流程** | 含随机抽样 / 模型生成（temperature>0），重跑**条条不同但统计量相近** | `datasets_clean_json.py`（无种子）、Ollama 生成 |

> 本库两种都有，**写的时候必须分开**，不要笼统说「可复现」。

> 📌 **数据卡是活文档** —— 数据集改了、或发现新缺陷，**回来改它**。
> 若某节确实用不上（例如来源未确认导致 §七 写不出命令），**写明原因**，不要留空、也不要编。
